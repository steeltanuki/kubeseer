{{- $labels := include "kubeseer.labels" . -}}
{{- $ownership := include "kubeseer.ownershipAnnotations" . -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kubeseer.fullname" . }}-preflight-hook
  namespace: {{ include "kubeseer.namespace" . }}
  labels:
    {{- $labels | nindent 4 }}
  annotations:
    {{- $ownership | nindent 4 }}
    helm.sh/hook: pre-install,pre-upgrade,pre-rollback
    helm.sh/hook-weight: "-40"
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "kubeseer.fullname" . }}-preflight-hook
  labels:
    {{- $labels | nindent 4 }}
  annotations:
    {{- $ownership | nindent 4 }}
    helm.sh/hook: pre-install,pre-upgrade,pre-rollback
    helm.sh/hook-weight: "-40"
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
rules:
  - apiGroups: ["apiextensions.k8s.io"]
    resources: ["customresourcedefinitions"]
    verbs: ["get"]
  - apiGroups: ["kubeseer.io"]
    resources: ["kubeseeraccesspolicies"]
    verbs: ["get"]
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get"]
  - apiGroups: ["admissionregistration.k8s.io"]
    resources: ["validatingwebhookconfigurations"]
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "kubeseer.fullname" . }}-preflight-hook
  labels:
    {{- $labels | nindent 4 }}
  annotations:
    {{- $ownership | nindent 4 }}
    helm.sh/hook: pre-install,pre-upgrade,pre-rollback
    helm.sh/hook-weight: "-40"
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "kubeseer.fullname" . }}-preflight-hook
subjects:
  - kind: ServiceAccount
    name: {{ include "kubeseer.fullname" . }}-preflight-hook
    namespace: {{ include "kubeseer.namespace" . }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kubeseer.fullname" . }}-verify-hook
  namespace: {{ include "kubeseer.namespace" . }}
  labels:
    {{- $labels | nindent 4 }}
  annotations:
    {{- $ownership | nindent 4 }}
    helm.sh/hook: post-install,post-upgrade,post-rollback
    helm.sh/hook-weight: "5"
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "kubeseer.fullname" . }}-verify-hook
  labels:
    {{- $labels | nindent 4 }}
  annotations:
    {{- $ownership | nindent 4 }}
    helm.sh/hook: post-install,post-upgrade,post-rollback
    helm.sh/hook-weight: "5"
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
rules:
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get"]
  - apiGroups: [""]
    resources: ["services"]
    verbs: ["get"]
  - apiGroups: ["admissionregistration.k8s.io"]
    resources: ["validatingwebhookconfigurations"]
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "kubeseer.fullname" . }}-verify-hook
  labels:
    {{- $labels | nindent 4 }}
  annotations:
    {{- $ownership | nindent 4 }}
    helm.sh/hook: post-install,post-upgrade,post-rollback
    helm.sh/hook-weight: "5"
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "kubeseer.fullname" . }}-verify-hook
subjects:
  - kind: ServiceAccount
    name: {{ include "kubeseer.fullname" . }}-verify-hook
    namespace: {{ include "kubeseer.namespace" . }}
