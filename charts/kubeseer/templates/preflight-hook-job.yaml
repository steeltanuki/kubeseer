apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "kubeseer.fullname" . }}-preflight
  namespace: {{ include "kubeseer.namespace" . }}
  labels:
    {{- include "kubeseer.labels" . | nindent 4 }}
  annotations:
    {{- include "kubeseer.ownershipAnnotations" . | nindent 4 }}
    helm.sh/hook: pre-install,pre-upgrade,pre-rollback
    helm.sh/hook-weight: "-35"
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
spec:
  backoffLimit: 0
  activeDeadlineSeconds: 120
  template:
    metadata:
      labels:
        {{- include "kubeseer.labels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "kubeseer.fullname" . }}-preflight-hook
      automountServiceAccountToken: true
      restartPolicy: Never
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: preflight
          image: {{ include "kubeseer.image" . | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["/kubeseer"]
          args:
            - package
            - preflight
            - --release-name={{ .Release.Name }}
            - --release-namespace={{ .Release.Namespace }}
            - --deployment-name={{ include "kubeseer.fullname" . }}
            - --webhook-service={{ include "kubeseer.webhookServiceName" . }}
            - --certificate-mode={{ .Values.certificate.mode }}
            - --expected-version={{ .Chart.AppVersion }}
            - --expected-kubeseer-crd-storage-version=v1alpha1
            - --expected-access-policy-crd-storage-version=v1alpha1
            - --webhook-port={{ .Values.manager.webhookPort }}
            - --timeout=90s
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
