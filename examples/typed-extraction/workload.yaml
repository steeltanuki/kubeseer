# Copyright 2026 Alessandro Rontani
# Licensed under the Apache License, Version 2.0.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: typed
  namespace: kubeseer-example-typed
  labels:
    kubeseer.io/example: typed-extraction
    app.kubernetes.io/name: kubeseer-example-typed
spec:
  replicas: 2
  selector:
    matchLabels: {app.kubernetes.io/name: kubeseer-example-typed}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: kubeseer-example-typed
        kubeseer.io/example: typed-extraction
    spec:
      containers: [{name: fixture, image: registry.k8s.io/pause:3.10, imagePullPolicy: IfNotPresent}]
