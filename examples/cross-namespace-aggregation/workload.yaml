# Copyright 2026 Alessandro Rontani
# Licensed under the Apache License, Version 2.0.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: aggregation-a
  namespace: kubeseer-example-aggregation-a
  labels: {kubeseer.io/example: cross-namespace-aggregation, app.kubernetes.io/name: aggregation-a}
spec:
  replicas: 1
  selector: {matchLabels: {app.kubernetes.io/name: aggregation-a}}
  template:
    metadata: {labels: {app.kubernetes.io/name: aggregation-a, kubeseer.io/example: cross-namespace-aggregation}}
    spec: {containers: [{name: fixture, image: registry.k8s.io/pause:3.10, imagePullPolicy: IfNotPresent}]}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: aggregation-b
  namespace: kubeseer-example-aggregation-b
  labels: {kubeseer.io/example: cross-namespace-aggregation, app.kubernetes.io/name: aggregation-b}
spec:
  replicas: 2
  selector: {matchLabels: {app.kubernetes.io/name: aggregation-b}}
  template:
    metadata: {labels: {app.kubernetes.io/name: aggregation-b, kubeseer.io/example: cross-namespace-aggregation}}
    spec: {containers: [{name: fixture, image: registry.k8s.io/pause:3.10, imagePullPolicy: IfNotPresent}]}
